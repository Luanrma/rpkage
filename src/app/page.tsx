'use client'

import styled from "styled-components";

const HomePageStyle = styled.div`
	display: flex;
	padding: 0.8rem;
`;

export default function HomePage() {

	return (
		<HomePageStyle>
			Home
		</HomePageStyle>
	);
}
